# Vercel Deployment Configuration

# The URL of your deployed Render backend (no trailing slash).
# If running locally, this can be omitted and it will default to http://localhost:5000/api
VITE_API_URL=https://resumize-backend.onrender.com/api
