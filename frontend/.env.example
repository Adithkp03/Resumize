# Vercel Deployment Configuration

# The URL of your deployed backend API (no trailing slash).
# MUST include the /api prefix to match the backend route: app.use('/api/resume', ...)
# For local development: VITE_API_URL=http://localhost:5000/api
VITE_API_URL=https://resumize-kappa.vercel.app/api
